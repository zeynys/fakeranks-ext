#include <playerutilsclass.h>
#include <entrypoint.h>

PlayerUtils::PlayerUtils(std::string m_plugin_name)
{
    this->plugin_name = m_plugin_name;
}

PlayerUtils::SetBunnyhop(int playerid, bool state)
{
    
}